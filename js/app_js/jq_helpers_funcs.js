// Seta os tooltips, deve ser chamado sempre que uma requisição remota acontecer e um tooltip for gerado
function preparar_tooltips(){
    /*
    $('.tooltiper').attr('rel', 'tooltip');
    $('.tooltiper').attr('data-placement', 'top');
    setTimeout(function(){
        $(".tooltiper").tooltip({trigger: "hover"});
    }, 1000);
    */
}